#include <stdio.h>
int main() {
    char ch;
    printf("");
    scanf("%c", &ch);
    if(isupper(ch)) {printf("Uppercase");}
    if else (islower(ch)) {printf("Lowercase");}
    else { printf("Not an alphabet")}
    return 0;
}