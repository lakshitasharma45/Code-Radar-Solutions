#include <stdio.h>
int main() {
    char ch;
    printf("");
    scanf("%c", &ch);
    if(isupper(ch)) {printf("Uppercase",  ch);}
    if else (islower(ch)) {printf("Lowercase", ch);}
    else { printf("Not an alphabet")}
    return 0;
}