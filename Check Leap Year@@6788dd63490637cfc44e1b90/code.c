#include <stdio.h>
int main() {
    int a;
    printf("");
    scanf("%d",&a);
    if ((a%4)  && (a%400)) {printf("Leap Year");}
    if else{ (a%100){printf("Not a Leap Year");}
    return 0;
}