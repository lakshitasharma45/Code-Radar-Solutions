#include <stdio.h>
int main () {
    int c,p;
    printf("");
    scanf("%d %d",&c,&p);
    if (s>c) {printf("Profit");}
    else if(c>s) {printf("Loss");}
    else {printf("No Profit or Loss");}
    return 0;
}